{
  "name": "eddie-canvas",
  "version": "0.1.0",
  "description": "Eddie - browser review canvas for agent-written plans and reports. Point, annotate, approve.",
  "license": "MIT",
  "bin": { "eddie": "./eddie.js" },
  "main": "lib/server.js",
  "engines": { "node": ">=18" },
  "scripts": { "test": "node tests/run-all.js" }
}
